# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.ruff_cache/

# uv
# (uv.lock IS committed — reproducible envs)

# Internal scaffolding (design/plan docs, agent guidance) — not repo deliverables
docs/superpowers/
.superpowers/
CLAUDE.md

# Training / experiment outputs (checkpoints are runtime artifacts, never committed)
outputs/
examples/outputs/
checkpoints/
wandb/
*.log

# Rendered scene screenshots & videos
renders/
videos/
*.mp4
results/

# MuJoCo runtime warning log (written to cwd on attach conflicts, EGL, etc.)
MUJOCO_LOG.TXT

# tools/ is mostly local scratch; ship only the user-facing ones.
tools/*
!tools/render_policy.py
!tools/eval_policy.py
!tools/benchmark_env.py
!tools/rank_series.py
!tools/plot_curves.py
tests/test_eval_render_size.py
