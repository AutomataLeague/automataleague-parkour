# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.ruff_cache/

# uv
# (uv.lock IS committed — reproducible envs)

# Internal scaffolding (design/plan docs, agent guidance) — not repo deliverables
docs/superpowers/
CLAUDE.md

# Training / experiment outputs (checkpoints are runtime artifacts, never committed)
outputs/
examples/outputs/
checkpoints/
wandb/
*.log

# Rendered scene screenshots & videos
renders/
videos/
*.mp4
results/

# MuJoCo runtime warning log (written to cwd on attach conflicts, EGL, etc.)
MUJOCO_LOG.TXT

# Dev tools (render/eval/leaderboard) — kept out of the training-facing repo
tools/
tests/test_eval_render_size.py
